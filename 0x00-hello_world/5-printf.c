#include <stdio.h>

/**
 * main - A program that prints a line using the prinf function
 * Return: 0 (Success)
 */
int main(void)
{
	printf("with proper grammer, but the outcome is a piece of art,\n");
	return (0);
}
